{
  "name": "codearena",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "dev:all": "concurrently \"npm run dev --workspace client\" \"npm run dev --workspace server\" \"npm run dev --workspace admin\"",
    "dev:admin": "npm run dev --workspace admin"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "express-rate-limit": "^8.5.2",
    "rate-limit-redis": "^5.0.0",
    "uuid": "^14.0.1",
    "yjs": "^13.6.31"
  }
}

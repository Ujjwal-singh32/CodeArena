# Admin panel — separate from main app
ADMIN_PORT=5001

# Main CodeArena API (where problems are uploaded)
API_URL=http://localhost:5000/api/v1

# Must match ADMIN_API_KEY in the main server .env
ADMIN_API_KEY=

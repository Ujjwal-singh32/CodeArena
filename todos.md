1. apply redis in problem like when we are clicking the problem then it is takign time
2. topic progess bar increasing on each submission
3. problem filter by question ==> use bloom filers or indexing and add debouncing in searching

4. start kafka on each thing...
5. after completing kafka set the api verison so that in deployment we can use v1 and on localhost we can use v2 by changing a single variable from env


6. if user leaves after creating room and game is not started then close the room and if started then it leave then make the opponent as winner..
7. fix array_duels in duel section..
8. display game name inside duel room   
9. display streak in dashboard....
10. rate limiting in submi button.

